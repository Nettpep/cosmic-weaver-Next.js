# 🔐 Admin CMS Setup Guide

## สารบัญ
1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [การติดตั้ง](#การติดตั้ง)
3. [การสร้าง Admin User](#การสร้าง-admin-user)
4. [การใช้งาน CMS](#การใช้งาน-cms)
5. [AI Features](#ai-features)
6. [Troubleshooting](#troubleshooting)

---

## ภาพรวมระบบ

ระบบ CMS นี้ประกอบด้วย:
- 🔐 **Authentication**: ใช้ Supabase Auth
- 📝 **Blog Editor**: WYSIWYG editor พร้อม live preview
- 🤖 **AI Assistant**: ใช้ Google Gemini 2.0 Flash
- 🔒 **Row-Level Security**: ป้องกันการเข้าถึงข้อมูล

---

## การติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
npm install @google/generative-ai
```

### 2. ตั้งค่า Environment Variables

เพิ่มใน `.env.local`:

```env
# Supabase (มีอยู่แล้ว)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Google Gemini API (ใหม่)
GEMINI_API_KEY=your_gemini_api_key
```

**วิธีขอ Gemini API Key:**
1. ไปที่ https://makersuite.google.com/app/apikey
2. คลิก "Create API Key"
3. Copy key มาใส่ใน `.env.local`

### 3. รัน Migration สำหรับ RLS

```bash
# ถ้าใช้ Supabase CLI
supabase db push

# หรือ copy SQL จาก supabase/migrations/20260206_admin_rls_policies.sql
# ไปรันใน Supabase Dashboard > SQL Editor
```

---

## การสร้าง Admin User

### วิธีที่ 1: ผ่าน Supabase Dashboard

1. ไปที่ **Supabase Dashboard > Authentication > Users**
2. คลิก **"Add User"**
3. กรอก Email และ Password
4. คลิก **"Create User"**
5. คลิกที่ user ที่สร้าง > **"Raw User Meta Data"**
6. เพิ่ม JSON:
   ```json
   {
     "role": "admin"
   }
   ```
7. Save

### วิธีที่ 2: ผ่าน SQL

รันใน **SQL Editor**:

```sql
-- สร้าง user ใหม่
INSERT INTO auth.users (
  instance_id,
  id,
  aud,
  role,
  email,
  encrypted_password,
  email_confirmed_at,
  raw_user_meta_data,
  created_at,
  updated_at
) VALUES (
  '00000000-0000-0000-0000-000000000000',
  gen_random_uuid(),
  'authenticated',
  'authenticated',
  'admin@cosmicweaver.com', -- เปลี่ยนเป็น email ของคุณ
  crypt('your_password_here', gen_salt('bf')), -- เปลี่ยนเป็น password ของคุณ
  now(),
  '{"role": "admin"}',
  now(),
  now()
);
```

---

## การใช้งาน CMS

### เข้าสู่ระบบ

1. ไปที่ `/admin/login`
2. กรอก Email และ Password ของ admin
3. ระบบจะพาไปที่ `/admin/blog` อัตโนมัติ

### หน้า Blog List (`/admin/blog`)

แสดงรายการบทความทั้งหมด พร้อม:
- ✅ สถานะ (เผยแพร่แล้ว / แบบร่าง)
- 📅 วันที่เผยแพร่
- 🏷️ หมวดหมู่
- ✏️ ปุ่มแก้ไข / ลบ

### หน้า Editor (`/admin/blog/new` หรือ `/admin/blog/[id]`)

#### ฟิลด์หลัก:
- **ชื่อบทความ** (required)
- **URL Slug** (auto-generate จากชื่อ)
- **คำโปรยสั้น** (Excerpt)
- **เนื้อหาบทความ** (required)
- **URL รูปปก**
- **สารจากผู้พิทักษ์** (Watcher's Insight)
- **เผยแพร่ทันที** (checkbox)

#### ปุ่มด้านบน:
- 👁️ **แสดงตัวอย่าง**: เปิด/ปิด live preview
- 💾 **บันทึก**: บันทึกบทความ

---

## AI Features

### 1. AI Co-Author (สำหรับเนื้อหา)

คลิกปุ่ม **"✨ AI Assist"** ข้างเนื้อหาบทความ

**โหมดที่มี:**

#### 📝 ขยายเนื้อหา
- ขยายเนื้อหาให้ยาวขึ้น 2-3 ย่อหน้า
- เพิ่มรายละเอียดและตัวอย่าง
- คงสไตล์เดิม

#### 🔄 คิดจุดหักมุม
- สร้างมุมมองใหม่ที่น่าประหลาดใจ
- เพิ่มจุดหักมุมที่ไม่คาดคิด
- กระตุ้นความคิดผู้อ่าน

#### 🌙 ปรับโทนลึกลับ
- ปรับภาษาให้ลึกลับมากขึ้น
- ใช้โทน "ผู้เฝ้ามอง"
- เพิ่มบรรยากาศน่าพรั่นพรึง

### 2. AI Metadata Generator

คลิกปุ่ม **"✨ AI สร้างให้"** ข้างคำโปรยสั้น

**สร้างให้:**
- URL Slug ที่เหมาะสม
- Excerpt สั้นๆ 1-2 ประโยค
- แท็กที่เกี่ยวข้อง 3-5 แท็ก

### 3. The Watcher's Insight Generator

คลิกปุ่ม **"✨ AI สร้างให้"** ข้างสารจากผู้พิทักษ์

**สร้าง:**
- ประโยคลึกลับ 1-2 ประโยค
- สไตล์ "ผู้เฝ้ามอง"
- กระตุ้นความคิดแต่ไม่สปอยล์

---

## Workflow แนะนำ

### สร้างบทความใหม่:

1. **เขียนชื่อบทความ**
   - Slug จะ auto-generate

2. **เขียนเนื้อหาหลัก**
   - เขียนแค่ย่อหน้าแรกก็ได้

3. **ใช้ AI ขยายเนื้อหา**
   - เลือกโหมด "ขยายเนื้อหา"
   - AI จะเสนอเนื้อหาเพิ่ม
   - คุณสามารถแก้ไขได้

4. **ใช้ AI สร้าง Metadata**
   - AI จะสร้าง Slug + Excerpt ให้

5. **ใช้ AI สร้าง Watcher's Insight**
   - AI จะสร้างข้อความลึกลับให้

6. **เปิด Preview ตรวจสอบ**

7. **บันทึก**
   - เลือก "เผยแพร่ทันที" หรือเก็บเป็น "แบบร่าง"

---

## Troubleshooting

### ❌ "อีเมลหรือรหัสผ่านไม่ถูกต้อง"

**สาเหตุ:**
- Email/Password ผิด
- User ยังไม่ได้สร้าง
- User ไม่มี role "admin"

**แก้ไข:**
1. ตรวจสอบ Email/Password
2. ตรวจสอบ `raw_user_meta_data` ใน Supabase
3. ต้องมี `{"role": "admin"}`

### ❌ "เกิดข้อผิดพลาดในการบันทึก"

**สาเหตุ:**
- RLS policies ยังไม่ได้รัน
- User ไม่มีสิทธิ์

**แก้ไข:**
1. รัน migration จาก `supabase/migrations/20260206_admin_rls_policies.sql`
2. ตรวจสอบว่า user มี role "admin"

### ❌ "AI request failed"

**สาเหตุ:**
- `GEMINI_API_KEY` ไม่ถูกต้อง
- API quota หมด
- Network error

**แก้ไข:**
1. ตรวจสอบ `.env.local` มี `GEMINI_API_KEY`
2. ตรวจสอบ API key ที่ https://makersuite.google.com/app/apikey
3. ตรวจสอบ quota

### ❌ บันทึกแล้วแต่ไม่เห็นในหน้าบล็อก

**สาเหตุ:**
- ยังไม่ได้เลือก "เผยแพร่ทันที"

**แก้ไข:**
1. แก้ไขบทความ
2. เลือก checkbox "เผยแพร่ทันที"
3. บันทึกอีกครั้ง

---

## Security Best Practices

1. **ใช้ Strong Password**
   - อย่างน้อย 12 ตัวอักษร
   - ผสม ตัวพิมพ์ใหญ่/เล็ก + ตัวเลข + สัญลักษณ์

2. **เก็บ API Keys ปลอดภัย**
   - ห้ามใส่ใน Git
   - ใช้ Environment Variables เท่านั้น

3. **จำกัดจำนวน Admin**
   - สร้างแค่ admin ที่จำเป็น
   - ถ้าไม่ใช้แล้วให้ลบ

4. **ตรวจสอบ Logs เป็นประจำ**
   - ดูใน Supabase Dashboard > Logs
   - ตรวจสอบการเข้าถึงที่ผิดปกติ

---

## คำถามที่พบบ่อย

**Q: สามารถมี admin หลายคนได้ไหม?**  
A: ได้ สร้าง user ใหม่และเพิ่ม `{"role": "admin"}` ใน `raw_user_meta_data`

**Q: AI ตอบภาษาอังกฤษ ทำยังไง?**  
A: ลองเขียนเนื้อหาเป็นภาษาไทยก่อน AI จะตอบภาษาไทยตาม

**Q: สามารถใช้ AI อื่นแทน Gemini ได้ไหม?**  
A: ได้ แก้ไขไฟล์ `app/api/ai-blog-tools/route.ts` เปลี่ยนเป็น OpenAI, Claude, etc.

**Q: จะเพิ่มหมวดหมู่ (Category) ยังไง?**  
A: ตอนนี้ต้องเพิ่มผ่าน Supabase Dashboard > Table Editor > categories

---

## ติดต่อ / Support

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Browser Console (F12)
2. ดู Supabase Logs
3. ตรวจสอบ `.env.local`

---

**สร้างโดย:** Cosmic Weaver Team  
**เวอร์ชัน:** 1.0.0  
**อัพเดทล่าสุด:** 6 ก.พ. 2026
